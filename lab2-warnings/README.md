# Лабораторная работа №2: Работа с ошибками и предупреждениями компилятора

## Цель работы
Научиться понимать и исправлять предупреждения компилятора, освоить флаги компиляции -Wall, -Wextra, -Werror, получить практический опыт работы с типичными ошибками языка C.

---

## Теоретическая справка: флаги компиляции GCC

### Зачем нужны флаги?
Компилятор GCC поддерживает множество опций, которые управляют процессом компиляции. Особое место занимают флаги диагностики — они включают вывод предупреждений о потенциальных проблемах в коде.

### Основные флаги диагностики

#### -Wall
Этот флаг включает большинство предупреждений компилятора. Название может вводить в заблуждение — это не "все предупреждения", а скорее "основной набор". -Wall включает такие проверки как:
- Неиспользуемые переменные
- Сравнение знаковых и беззнаковых типов
- Отсутствие возвращаемого значения в функции
- Неинициализированные переменные
- И многие другие

#### -Wextra
Включает дополнительные предупреждения, не входящие в -Wall. Например:
- Сравнение с всегда истинным/ложным выражением
- Пустое тело условного оператора
- Избыточные операции
- Потеря точности при неявном приведении типов

#### -Werror
Превращает все предупреждения в ошибки. Это полезно в продакшн-коде, так как заставляет разработчика исправлять все предупреждения до компиляции.

#### Другие полезные флаги
- -Wpedantic — предупреждения о нарушении стандартов ISO C
- -Wconversion — предупреждения о неявных преобразованиях, меняющих значения
- -Wshadow — предупреждения о переменных, скрывающих другие переменные

### Пример использования
Компиляция с основными предупреждениями:

gcc -Wall program.c -o program

Компиляция с дополнительными предупреждениями:

gcc -Wall -Wextra program.c -o program

Компиляция с превращением предупреждений в ошибки:

gcc -Wall -Wextra -Werror program.c -o program

---

## Порядок выполнения лабораторной работы

### 1. Выберите решение из лабораторной работы №1
Возьмите одно из двух заданий, которые вы выполняли в первой лабораторной работе. Это может быть любое задание, которое вы сдали или находитесь в процессе сдачи.

### 2. Создайте копию для второй лабораторной работы

Перейдите в папку второй лабораторной работы:

cd lab2-warnings

Создайте свою ветку:

git checkout -b student-{ваш_номер}-lab2

Создайте папку для решения:

mkdir -p solutions/student-{ваш_номер}

Скопируйте ваш код из первой лабораторной:

cp ../lab1-c-basics/solutions/student-{ваш_номер}/task{номер_задания}/main.c solutions/student-{ваш_номер}/main.c

### 3. Изучите код без предупреждений
Сначала скомпилируйте ваш код с флагами -Wall -Wextra, чтобы убедиться, что он чистый:

gcc -std=c99 -Wall -Wextra solutions/student-{ваш_номер}/main.c -o /tmp/clean

Если код из первой лабораторной был написан правильно, компиляция должна пройти без единого предупреждения. Если предупреждения есть — исправьте их.

### 4. Добавьте в код предупреждения (основное задание)
Модифицируйте код, чтобы в нем появилось не менее 10 разных типов предупреждений, обнаруживаемых флагом -Wall.

Важно: Программа должна оставаться работоспособной и корректно выполнять исходное задание. Предупреждения не должны влиять на логику работы.

Важно: Код должен компилироваться. Флаг -Werror НЕ ИСПОЛЬЗУЕТСЯ на этом этапе.

### 5. Добавьте бонусные предупреждения (бонусное задание)
Дополнительно добавьте не менее 5 предупреждений, которые обнаруживаются только флагом -Wextra.

### 6. Задокументируйте предупреждения
Создайте файл warnings.md в папке с решением, где для каждого предупреждения укажите:
- Строку кода, вызывающую предупреждение
- Текст предупреждения компилятора
- Краткое объяснение, почему возникает это предупреждение

### 7. Компиляция и тестирование

Компиляция для проверки предупреждений -Wall:

gcc -std=c99 -Wall solutions/student-{ваш_номер}/main.c -o solutions/student-{ваш_номер}/program

Компиляция для проверки предупреждений -Wextra:

gcc -std=c99 -Wall -Wextra solutions/student-{ваш_номер}/main.c -o solutions/student-{ваш_номер}/program

Запуск программы:

./solutions/student-{ваш_номер}/program

### 8. Отправка решения

git add solutions/student-{ваш_номер}/
git commit -m "Lab 2 solution by student {номер}"
git push origin student-{ваш_номер}-lab2

Создайте Pull Request в ветку main репозитория преподавателя.

---

## Типы предупреждений (для справки)

### Предупреждения -Wall
- -Wunused-variable — неиспользуемая переменная
- -Wunused-parameter — неиспользуемый параметр функции
- -Wunused-function — неиспользуемая статическая функция
- -Wreturn-type — отсутствие return в функции
- -Wsign-compare — сравнение знакового и беззнакового
- -Wuninitialized — использование неинициализированной переменной
- -Wparentheses — отсутствие скобок в сложных выражениях
- -Wimplicit-int — неявное определение типа
- -Wimplicit-function-declaration — использование функции без объявления
- -Wmain — некорректная сигнатура main
- -Wmisleading-indentation — вводящий в заблуждение отступ
- -Wempty-body — пустое тело if/else/while
- -Wpointer-sign — несоответствие указателей по знаковости
- -Wdiv-by-zero — деление на ноль
- -Wchar-subscripts — использование char как индекса массива
- -Wsizeof-pointer-memaccess — подозрительное использование sizeof с указателями

### Предупреждения -Wextra (для бонуса)
- -Wtype-limits — сравнение, которое всегда истинно/ложно
- -Wclobbered — переменная могла быть изменена
- -Wcast-align — выравнивание может быть нарушено при приведении
- -Wmissing-field-initializers — пропущены поля при инициализации структуры
- -Wmaybe-uninitialized — возможно использование неинициализированной переменной
- -Wunused-but-set-variable — переменная присваивается, но не используется
- -Wunused-but-set-parameter — параметру присваивается, но не используется
- -Woverflow — переполнение при константных вычислениях
- -Wshift-count-overflow — сдвиг на количество бит, превышающее размер типа

---

## Требования к оформлению

### Структура файла main.c
В начале файла должен быть комментарий с информацией о студенте:

/*
 * Лабораторная работа №2
 * Студент: Иванов Иван (номер: 12)
 * Вариант: 5
 * Задание: 5 (N-е число Фибоначчи)
 * Количество предупреждений: 12 (из них 5 -Wextra)
 */

### Структура файла warnings.md
Создайте файл warnings.md в той же папке, где лежит main.c. Пример содержания:

# Отчет о предупреждениях
Студент: 12
Задание: 5 (N-е число Фибоначчи)

## Предупреждения -Wall

### 1. Неиспользуемая переменная
- Строка: 25
- Код: int unused_counter = 0;
- Текст предупреждения: warning: unused variable 'unused_counter' [-Wunused-variable]
- Объяснение: Переменная объявлена и инициализирована, но нигде не используется.

### 2. Отсутствие return в функции
- Строка: 42
- Код: int get_value() { }
- Текст предупреждения: warning: control reaches end of non-void function [-Wreturn-type]
- Объяснение: Функция объявлена как возвращающая int, но не содержит return.

(и так далее для всех 10+ предупреждений)

## Предупреждения -Wextra (бонус)

### 1. Сравнение с всегда истинным выражением
- Строка: 67
- Код: unsigned int x = 5; if (x >= 0)
- Текст предупреждения: warning: comparison of unsigned expression >= 0 is always true [-Wtype-limits]
- Объяснение: Для беззнакового типа условие x >= 0 всегда истинно.

(и так далее для всех 5+ предупреждений)

### Основные требования к коду
- Код должен компилироваться (с флагами -Wall -Wextra, но без -Werror)
- Программа должна работать корректно и выполнять исходное задание
- Предупреждения не должны влиять на логику работы
- Запрещено использовать #pragma для подавления предупреждений
- Каждое предупреждение должно быть осмысленным и соответствовать своему типу

---

## Критерии оценивания

Итоговая оценка = 5 - количество баллов, снятых за нарушения

### Основная часть (максимум 5 баллов)
- 5 баллов — не менее 10 разных типов предупреждений, все корректно описаны, программа работает
- 4 балла — 8-9 предупреждений или неполное описание
- 3 балла — 5-7 предупреждений
- 0 баллов — менее 5 предупреждений

### Бонусная часть (максимум +1 балл, но не более 5 в сумме с основной)
- +1 балл — добавлено не менее 5 предупреждений -Wextra с корректным описанием

### Штрафы
- -5 баллов — программа не компилируется
- -1 балл — программа работает некорректно (изменена логика)
- -1 балл — отсутствует или неполный файл warnings.md
- -1 балл — повторяющиеся типы предупреждений

---

## Сроки сдачи

| Этап | Дата |
|------|------|
| Дата выдачи | 21.02.2026 |
| Дедлайн сдачи (PR создан) | 14.03.2026 23:59 |
| Период ревью | 21.02.2026 - 14.03.2026 |

Важно: За работы, сданные после дедлайна, за каждую полную или неполную неделю просрочки снимается 1 балл.

---

## Пример структуры репозитория после выполнения
```
lab2-warnings/
├── solutions/
│   ├── student-12/
│   │   ├── main.c
│   │   └── warnings.md
└── README.md
```
---

Удачи в выполнении! 🚀 Понимание предупреждений компилятора — ключ к написанию качественного и безопасного кода на C.