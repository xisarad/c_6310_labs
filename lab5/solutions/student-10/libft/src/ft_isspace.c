//Проверка пробела	Это пробел, таб, перевод строки?	' '→1, '\t'→1, 'A'→0
#include "libft.h"

int	ft_isspace(int c)
{
	return (c == ' ' || c == '\t' || c == '\n' 
		|| c == '\v' || c == '\f' || c == '\r');
}