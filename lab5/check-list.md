# Чек-лист для самопроверки

## Тестирование

### Ручное тестирование
Запустите программу, проверьте корректность работы всех операций по вашему варианту. Примеры приведены для каждого варианта.

### Проверка утечек памяти
Используйте `valgrind`:
```
valgrind --leak-check=full ./matrix
```
Убедитесь, что все выделенные блоки освобождены.

### Сравнение с эталоном
Для проверки правильности работы можно сравнить вывод вашей программы с ручным расчётом, но в рамках лабораторной работы это не обязательно.

---

## Чек-лист

- [ ] Программа компилируется без ошибок и предупреждений с флагами `-Wall -Wextra -Werror`.
- [ ] В `Makefile` реализованы цели `all`, `clean`, `fclean`, `re`.
- [ ] Реализована библиотека `matrix`: заголовочный файл `matrix.h` и `.c`-файлы с реализациями.
- [ ] Реализованы базовые функции: `create_matrix`, `remove_matrix`, `print_matrix` с использованием структуры `matrix_t`.
- [ ] Функции по варианту (сложение/вычитание, умножение на число/транспонирование, умножение матриц или сравнение/анализ свойств) работают корректно.
- [ ] `main.c` содержит демонстрацию: создание матриц, вызов функций библиотеки, вывод результатов, освобождение памяти.
- [ ] Все операции с выводом выполняются через функции libft (`ft_putchar_fd`, `ft_putstr_fd`, `ft_putnbr_fd`, `ft_putendl_fd`).
- [ ] Функции библиотеки корректно обрабатывают ошибки (нулевые/отрицательные размеры, несовместимые матрицы, невозможность выделить память) и возвращают `NULL` или код ошибки.
- [ ] Нет утечек памяти (`valgrind` показывает "All heap blocks were freed").
- [ ] Код отформатирован, нет глобальных переменных, нет лишних включений заголовочных файлов.

---

## Критерии оценивания (максимум 5 баллов)

| Критерий | Баллы |
|----------|-------|
| Реализована библиотека `matrix`: заголовочный файл `matrix.h` и `.c`-файлы с реализациями функций | 1 |
| Реализованы базовые функции: `create_matrix`, `remove_matrix`, `print_matrix` с использованием структуры `matrix_t` (корректное выделение и освобождение памяти) | 1 |
| Операции по выбранному варианту реализованы и работают корректно (сложение/вычитание, умножение на число/транспонирование, умножение матриц или сравнение/анализ свойств) | 1 |
| `main.c` содержит демонстрацию работы библиотеки: создание матриц, вызовы функций, вывод через libft, освобождение памяти; запрещённые стандартные функции (`printf`, `puts`, `atoi`, `malloc`, `free` и т.д.) в `main.c` не используются | 1 |
| Отсутствие утечек памяти (проверка `valgrind`), корректное освобождение всей выделенной памяти | 1 |
| **Итого** | **5** |

### Штрафы:
- Использование любой запрещённой стандартной функции (например, `printf`, `strlen`, `atoi`, `malloc`, `free` и т.п.) в `main.c` — работа не принимается (**0 баллов**).
- Утечки памяти — **−1 балл**.
- Функции библиотеки не обрабатывают ошибки (нулевые/отрицательные размеры, несовместимые матрицы, невозможность выделить память) — **−1 балл**.
- Программа не компилируется с флагами `-Wall -Wextra -Werror` — **−5 баллов**.
