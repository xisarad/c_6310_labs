# Лабораторная работа №1: Базовые алгоритмы на языке C

## Цель работы
Закрепить навыки работы с базовыми конструкциями языка C: переменные, типы данных, ввод/вывод, условные операторы, циклы, функции и одномерные массивы.

---

## Порядок выполнения лабораторной работы

### 1. Настройка репозитория
Убедитесь, что вы находитесь в своем локальном репозитории курса и синхронизировали его с удаленным:

git pull origin main

### 2. Перейдите в папку лабораторной работы

cd lab1-c-basics

### 3. Создайте свою ветку
Ветка должна быть создана от актуального состояния main:

git checkout -b student-{ваш_номер}-lab1

Пример: git checkout -b student-12-lab1

### 4. Создайте структуру папок для решений

mkdir -p solutions/student-{ваш_номер}/task{номер_задания1}
mkdir -p solutions/student-{ваш_номер}/task{номер_задания2}

Пример для варианта 5 (задания 5 и 20):

mkdir -p solutions/student-12/task05
mkdir -p solutions/student-12/task20

### 5. Определите свой вариант
Номер варианта = (ваш_номер_студента % 25) + 1 (если остаток 0, то вариант 25).

| Вариант | Задание 1 | Задание 2 |
|---------|-----------|-----------|
| 1 | 1 | 16 |
| 2 | 2 | 17 |
| 3 | 3 | 18 |
| 4 | 4 | 19 |
| 5 | 5 | 20 |
| 6 | 6 | 21 |
| 7 | 7 | 22 |
| 8 | 8 | 23 |
| 9 | 9 | 24 |
| 10 | 10 | 25 |
| 11 | 11 | 26 |
| 12 | 12 | 27 |
| 13 | 13 | 28 |
| 14 | 14 | 29 |
| 15 | 15 | 30 |
| 16 | 16 | 1 |
| 17 | 17 | 2 |
| 18 | 18 | 3 |
| 19 | 19 | 4 |
| 20 | 20 | 5 |
| 21 | 21 | 6 |
| 22 | 22 | 7 |
| 23 | 23 | 8 |
| 24 | 24 | 9 |
| 25 | 25 | 10 |

### 6. Выполните задания
В каждой папке taskXX создайте файл main.c и реализуйте соответствующее задание. Каждое задание — это отдельная программа со своей функцией main().

---

## Список заданий

1. Сумма элементов массива
2. Нахождение наибольшего числа в массиве
3. Сортировка массива по возрастанию
4. Факториал числа
5. N-е число Фибоначчи
6. Среднее арифметическое массива
7. Проверка числа на простоту
8. Наибольший общий делитель двух чисел
9. Проверка строки на палиндром
10. Перевод десятичного числа в двоичное
11. Сумма ряда 1 + 1/2 + 1/3 + ... + 1/n
12. Проверка числа на палиндром в двоичной системе
13. Сумма цифр числа
14. Среднее значение вещественного массива
15. Проверка вхождения числа в заданный диапазон
16. Сумма диагональных элементов матрицы
17. Наименьшее общее кратное двух чисел
18. Сумма первых n натуральных чисел
19. Вычисление значения функции y = x² + 2x + 1
20. Перевод символа в ASCII-код и обратно
21. Площадь круга по радиусу
22. Корни квадратного уравнения
23. Факториал числа с использованием цикла while
24. Проверка вхождения подстроки в строку
25. Сумма всех четных чисел в диапазоне от a до b
26. Проверка строки на панграмму
27. Сумма элементов каждого столбца матрицы
28. Корни квадратного уравнения с помощью функции
29. Сумма элементов главной и побочной диагоналей матрицы
30. Сумма элементов каждого столбца в матрице

---

## Компиляция и тестирование

### Компиляция каждого задания

Компиляция задания:

gcc -std=c99 -Wall -Wextra -Werror main.c -o program

Запуск:

./program

---

## Требования к оформлению кода

1. Структура файла:
   В начале каждого main.c — комментарий с информацией о студенте, варианте и номере задания:

   /*
    * Лабораторная работа №1
    * Студент: Иванов Иван (номер: 12)
    * Вариант: 5
    * Задание: 5 (N-е число Фибоначчи)
    */

   После #include — реализация функций.
   Каждый файл должен компилироваться в отдельную программу.

2. Стиль кодирования:
   - Имена переменных и функций должны быть осмысленными (не a, b, c, x, y).
   - Отступы — 4 пробела (не табуляция).
   - Фигурные скобки открываются на той же строке (K&R стиль):
```c
     if (condition) {
         do_something();
     }
```
   - Между операторами и операндами ставятся пробелы: 
   ```c 
   a + b, if (x > y).
   ```
   - Функции должны быть компактными (не более 30-40 строк).
   - Магические числа заменять именованными константами (#define или const).

3. Ввод/вывод:
   - Программа должна запрашивать данные у пользователя в понятной форме.
   - Выводить результаты с пояснениями.
   - Проверять корректность ввода (например, scanf вернул ожидаемое количество значений).

4. Обработка ошибок:
   - Проверять корректность ввода (деление на ноль, отрицательные значения для факториала, выход за границы массива).
   - При некорректном вводе выводить понятное сообщение об ошибке и завершать программу с кодом 1.

5. Компиляция:
   - Код должен компилироваться без ошибок и предупреждений с флагами:

     -std=c99 -Wall -Wextra -Werror

   - Запрещено использовать:
     - Глобальные переменные (кроме констант)
     - Закомментированные участки кода (dead code)

---

## Отправка решения

### 1. Добавьте файлы в git

git add solutions/student-{ваш_номер}/

### 2. Создайте коммит

git commit -m "Lab 1 solution by student {номер}: tasks {задание1} and {задание2}"

### 3. Запушьте изменения

git push origin student-{ваш_номер}-lab1

### 4. Создайте Pull Request
В интерфейсе GitLab/GitHub создайте PR из ветки student-{ваш_номер}-lab1 в ветку main репозитория преподователя. 
В названии PR укажите: 
```
Lab 1 solution | Student {номер} | Variant {вариант} 
```


## Шаблон решения 
```
/*
 * Лабораторная работа №1
 * Студент: {Фамилия Имя} (номер: {номер})
 * Вариант: {вариант}
 * Задание: {номер} ({название задания})
 */

#include <stdio.h>

/* Возможно, потребуются дополнительные библиотеки */
/* #include <math.h> */
/* #include <string.h> */

/* Объявления вспомогательных функций (если нужны) */

int main(void) {
    /* TODO: реализовать задание */
    
    printf("Задание %d еще не реализовано.\n", {номер});
    
    return 0;
}

/* Реализации вспомогательных функций (если нужны) */

```

## Сроки сдачи

| Этап | Дата |
|------|------|
| Дата выдачи | 21.02.2026 |
| Дедлайн сдачи (PR создан) | 28.02.2026 23:59 |
| Период ревью | 21.02.2026 - 07.03.2026 |

Важно: За работы, сданные после дедлайна, за каждую полную или неполную неделю просрочки снимается 1 балл.

---

## Пример структуры репозитория после выполнения
```text
lab1-c-basics/
├── templates/
│   └── task-template.c
├── tasks/
│   └── tasks.txt (описание заданий)
├── solutions/
│   ├── student-12/
│   │   ├── task05/
│   │   │   ├── main.c
│   │   │   └── program (исполняемый, в git не добавлять!)
│   │   └── task20/
│   │       ├── main.c
│   │       └── program (исполняемый, в git не добавлять!) 
├── tests/
│   └── (автоматические тесты)
└── README.md
```

Удачи в выполнении! 🚀 Каждое задание — отдельная программа, компилируйте и тестируйте их по отдельности.